# Use Git LFS for PyTorch model files. Run:
#   git lfs install
#   git lfs track "*.pt"
# Commit the generated .gitattributes file so LFS tracking is enabled for collaborators.

*.pt filter=lfs diff=lfs merge=lfs -text
models/*.[pP][tT] filter=lfs diff=lfs merge=lfs -text
